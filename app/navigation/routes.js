const ROUTE_AUTH = 'Route Auth';
const LOGIN = 'Login';

const ROUTE_TAB = 'Route Tab';
const TAB_HOME = 'Tab Home';
const HOME = 'Home';

const TAB_QR_CODE = 'Tab QRCode';
const QR_CODE = 'QRCode';

const TAB_USER = 'Tab User';
const USER = 'User';

const BOOK = 'Book';

const routes = {
	ROUTE_AUTH,
	LOGIN,
	ROUTE_TAB,
	TAB_HOME,
	HOME,

	TAB_QR_CODE,
	QR_CODE,
	TAB_USER,
	USER,
	BOOK,
};

export default routes;
